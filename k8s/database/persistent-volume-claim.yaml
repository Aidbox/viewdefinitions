apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: vd-designer-database-volume-claim
  namespace: viewdefinition

spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
